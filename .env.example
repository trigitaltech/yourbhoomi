# Google Analytics 4 measurement ID. Loads only after the visitor accepts analytics cookies.
NEXT_PUBLIC_GA_ID=

# OpenWA Easy API used to forward leads to the desk on WhatsApp (see src/lib/openwa.ts).
# Start: npx @open-wa/wa-automate -p 8080 -k YOUR_KEY
OPENWA_API_URL=http://localhost:8080
OPENWA_API_KEY=
OPENWA_TO=917013870575

# Leads are also appended to ./data/leads.jsonl and ./data/interest.jsonl (gitignored).
